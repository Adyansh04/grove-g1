<root BTCPP_format="4">
    <TreeNodesModel>
        <Action ID="AcquireArm">
            <input_port name="timeout_s" type="double" default="15.000000">Per-step service budget.</input_port>
        </Action>
        <Action ID="ApproachObject">
            <input_port name="timeout_s" type="double" default="0.000000">0 uses the server's own default.</input_port>
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="use_current_heading" type="bool" default="false">Hold whatever heading the robot already has, instead of working_yaw.</input_port>
            <input_port name="working_yaw" type="double">Heading to hold while approaching, usually NavigateToPose's goal_yaw; unused with use_current_heading.</input_port>
            <input_port name="arm" type="std::string" default="right">'left' or 'right'.</input_port>
            <input_port name="object_id" type="std::string">Must match a class_id published on /objects.</input_port>
        </Action>
        <Action ID="ClearCostmaps">
            <input_port name="local_service" type="std::string" default="/local_costmap/clear_entirely_local_costmap">Nav2 local costmap clear service. Empty skips it.</input_port>
            <input_port name="global_service" type="std::string" default="/global_costmap/clear_entirely_global_costmap">Nav2 global costmap clear service. Empty skips it.</input_port>
            <input_port name="timeout_s" type="double" default="5.000000">Per-costmap service budget.</input_port>
        </Action>
        <Action ID="ClearOctomap">
            <input_port name="service" type="std::string" default="/clear_octomap">move_group's octomap clear service. Empty skips it.</input_port>
            <input_port name="timeout_s" type="double" default="5.000000">Service budget.</input_port>
        </Action>
        <Action ID="Grasp">
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="object_id" type="std::string">Must match a class_id published on /objects.</input_port>
            <input_port name="arm" type="std::string" default="right">'left' or 'right'.</input_port>
            <input_port name="instruction" type="std::string">What the policy is asked to do, in plain language.</input_port>
        </Action>
        <Action ID="LookFor">
            <input_port name="timeout_s" type="double" default="20.000000">Seconds to wait for the objects once the phrases are written. The write gets up to 5 s of its own.</input_port>
            <input_port name="detector" type="std::string" default="/g1_detector">Node whose `phrases` to write. Empty waits without narrowing anything.</input_port>
            <input_port name="also" type="std::string" default="">More objects to ask for, in the same form, without waiting on them: ones a later step needs to see, like the object Place confirms after letting go.</input_port>
            <input_port name="objects" type="std::string">Comma-separated objects to ask the detector for and wait on: an id, or 'id=phrase' when the wording that finds it differs from the name the tree uses.</input_port>
        </Action>
        <Action ID="NavigateToPose">
            <output_port name="goal_yaw" type="double">The goal's heading, for an ApproachObject that has to hold it.</output_port>
            <input_port name="behavior_tree" type="std::string" default="">Nav2 tree to drive with. Empty uses its default.</input_port>
            <input_port name="frame_id" type="std::string" default="map">Frame the goal is expressed in.</input_port>
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="goal" type="g1_orchestration::Station">Where to drive to, as 'x;y;yaw'.</input_port>
        </Action>
        <Action ID="Pick">
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="arm" type="std::string" default="right">'left' or 'right'.</input_port>
            <input_port name="object_id" type="std::string">Must match a class_id published on /objects.</input_port>
        </Action>
        <Action ID="Place">
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="frame_id" type="std::string" default="">Frame of the target. Empty means the server's planning frame.</input_port>
            <input_port name="arm" type="std::string" default="right">'left' or 'right'.</input_port>
            <input_port name="target" type="g1_orchestration::Point3">Where the OBJECT should end up, as 'x;y;z'.</input_port>
            <input_port name="surface" type="std::string" default="">Detected surface to place ON TOP OF. Preferred over 'target'.</input_port>
        </Action>
        <Action ID="ReleaseArm">
            <input_port name="timeout_s" type="double" default="15.000000">Per-step service budget.</input_port>
        </Action>
        <Action ID="Retreat">
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="timeout_s" type="double" default="0.000000">0 uses the server's own default.</input_port>
            <input_port name="distance" type="double" default="0.600000">How far to reverse, in metres.</input_port>
        </Action>
        <Action ID="SetArmPosture">
            <input_port name="server_timeout_s" type="double" default="10.000000">How long to wait for the action server to appear.</input_port>
            <input_port name="named_target" type="std::string">A group_state of that group, e.g. tucked.</input_port>
            <input_port name="group" type="std::string">A group in g1.srdf, e.g. right_arm.</input_port>
        </Action>
        <Action ID="StopLooking">
            <input_port name="timeout_s" type="double" default="5.000000">Budget for the write.</input_port>
            <input_port name="detector" type="std::string" default="/g1_detector">Node whose `phrases` to empty.</input_port>
        </Action>
    </TreeNodesModel>
</root>
