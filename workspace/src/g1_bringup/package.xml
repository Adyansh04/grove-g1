<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>g1_bringup</name>
  <version>0.1.0</version>
  <description>
    Bring-up for the G1 stack: launches unitree_mujoco and the ros2_control stack and composes
    navigation, MoveIt, manipulation, perception and the VLA grasp onto them. Launch files,
    config, scenes and scripts only.
  </description>
  <maintainer email="gupta.adyansh@gmail.com">Adyansh</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>g1_description</depend>
  <depend>g1_state_estimation</depend>
  <depend>g1_sensor_relay</depend>
  <depend>robot_state_publisher</depend>
  <depend>controller_manager</depend>
  <depend>joint_state_broadcaster</depend>
  <depend>imu_sensor_broadcaster</depend>
  <depend>forward_command_controller</depend>
  <depend>g1_controllers</depend>
  <depend>joint_trajectory_controller</depend>
  <depend>xacro</depend>
  <depend>controller_manager_msgs</depend>
  <depend>lifecycle_msgs</depend>
  <depend>rclpy</depend>
  <depend>launch</depend>
  <depend>launch_ros</depend>

  <exec_depend>ament_index_python</exec_depend>
  <!-- activate_arm's clear-posture move goes out as a trajectory goal of its own. -->
  <exec_depend>control_msgs</exec_depend>
  <exec_depend>trajectory_msgs</exec_depend>
  <exec_depend>xvfb</exec_depend>
  <exec_depend>rviz2</exec_depend>

  <!-- ros2_control plugins, loaded at runtime from the URDF; nothing here compiles against
       them. -->
  <exec_depend>g1_hardware_interface</exec_depend>
  <exec_depend>g1_hand_interface</exec_depend>

  <!-- Manual teleop demo dependency (see README). -->
  <exec_depend>teleop_twist_keyboard</exec_depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_cmake_lint_cmake</test_depend>
  <test_depend>ament_cmake_xmllint</test_depend>
  <test_depend>launch_testing</test_depend>
  <test_depend>launch_testing_ament_cmake</test_depend>
  <test_depend>nav_msgs</test_depend>
  <test_depend>sensor_msgs</test_depend>
  <test_depend>geometry_msgs</test_depend>
  <test_depend>std_msgs</test_depend>
  <test_depend>tf2_msgs</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
